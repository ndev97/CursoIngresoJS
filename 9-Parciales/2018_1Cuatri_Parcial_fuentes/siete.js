function mostrar()
{
    var contador = 0;

    while (contador < 5) {
        contador++;
        
        do {
            var nota = prompt("Ingrese una nota");
            nota = parseInt(nota);
        } while (nota(isNaN) || nota < 0 || nota > 10);

        do {
            var sexo = prompt("Ingrese F o M");
        } while (sexo )
    }
}
